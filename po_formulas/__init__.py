"""po_formulas — formula packs built on top of prefect-orchestration core.

This package currently hosts the agent-messaging helper (`mail`). Concrete
formulas (software_dev, epic, deployments) are expected to land here in
sibling modules; see `prefect_orchestration/cli.py` for the entry-point
references.
"""
