# Baseline Notes

Pre-existing failure: `tests/test_agent_session_tmux.py::test_tmux_backend_passes_resume_across_turns` — assertion `len(turns) == 2` but got 1. Present before any changes.

16 passed, 1 failed.
