# Keeps `claude-context/` present in the tree so `Dockerfile` and the
# `--build-context claude-context=./claude-context` flow always resolve.
# Real content is generated by `scripts/sync-claude-context.sh` and
# git-ignored. Do not commit the populated tree — it would leak the
# developer's user-global Claude config into the repo.
